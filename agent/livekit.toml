# LiveKit Cloud agent configuration
# This file is auto-updated by `lk agent create` with the agent ID and project.
# Check it into source control after first deploy.
# Docs: https://docs.livekit.io/deploy/agents/quickstart/
#
# DEPLOY COMMANDS:
#   First time:  lk agent create --secrets MISTRAL_API_KEY=xxx,LINKUP_API_KEY=xxx
#   Updates:     lk agent deploy
#   Status:      lk agent status
#   Logs:        lk agent logs
